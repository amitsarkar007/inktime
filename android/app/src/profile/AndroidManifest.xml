<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- INTERNET is used in profile builds for tooling that talks to the running app. -->
    <uses-permission android:name="android.permission.INTERNET"/>
</manifest>
